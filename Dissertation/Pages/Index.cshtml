@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}
<!-- 
/img/Carousel/img-1.jpeg
/img/Carousel/img-2.jpg
/img/Carousel/img-3.png
-->
<div id="carouselExampleCaptions" class="carousel slide carousel-fade pb-3" data-bs-ride="true">
    <div class="carousel-indicators">
        <button type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide-to="0" class="active" aria-current="true" aria-label="Slide 1"></button>
        <button type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide-to="1" aria-label="Slide 2"></button>
        <button type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide-to="2" aria-label="Slide 3"></button>
    </div>
    <div class="carousel-inner">
        <div class="carousel-item active">
            <img src="/img/Carousel/img-1.jpeg" class="d-block w-100" alt="...">
            <div class="carousel-caption d-none d-md-block">
                <h5>World Robotics Olympiad</h5>
                <p>Lady Lorimer joined us to learn about building robots.</p>
            </div>
        </div>
        <div class="carousel-item">
            <img src="/img/Carousel/img-2.jpg" class="d-block w-100" alt="...">
            <div class="carousel-caption d-none d-md-block">
                <h5>Celebrating CyberCenturion</h5>
                <p>Our 2023 CyberCenturion Awards Night.</p>
            </div>
        </div>
        <div class="carousel-item">
            <img src="/img/Carousel/img-3.png" class="d-block w-100" alt="...">
            <div class="carousel-caption d-none d-md-block">
                <h5>Exploring Cyber-Space</h5>
                <p>Competing in the 2022 CyberCenturion National Finals.</p>
            </div>
        </div>
    </div>
    <button class="carousel-control-prev" type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide="prev">
        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
        <span class="visually-hidden">Previous</span>
    </button>
    <button class="carousel-control-next" type="button" data-bs-target="#carouselExampleCaptions" data-bs-slide="next">
        <span class="carousel-control-next-icon" aria-hidden="true"></span>
        <span class="visually-hidden">Next</span>
    </button>
</div>
<h2>Latest News</h2>
<div class="row news-row">
    @foreach (var item in Model.Article)
    {
        <div class="col col-12 col-sm-6 col-lg-4 pb-3 d-flex">
            <a asp-page="./News/Post" asp-route-id="@item.Id" class="d-flex">
                <div class="article-preview flex-grow-1">
                    <div class="banner-img-wrapper">
                        <img class="banner-image" src="@($" https://dissertationad.blob.core.windows.net/news-banner-images/{item.Id}.png")" alt="@item.Title" onerror="if (this.src != '/img/codeclublogo.png') this.src = '/img/codeclublogo.png';" />
                    </div>

                    <hr />
                    <h2>@item.Title</h2>
                    <div class="preview-wrapper px-2">
                        @{
                            var words = (item.Content ?? "").Split(' ').Take(50);
                            string output = "";
                            foreach (var word in words)
                            {
                                output += word + " ";
                            }
                            output = output.Trim();
                            if ((item.Content ?? "").Split(' ').Count() > 50)
                            {
                                output += "...";
                            }
                            <md-block>@output</md-block>
                        }
                    </div>
                </div>
            </a>
        </div>
    }
</div>
